# Repository instructions

## Cursor Cloud specific instructions

- This repository currently contains only `README.md`; there are no application sources, dependency manifests, build scripts, devcontainer files, or active Git hooks checked in.
- The VM startup dependency refresh script is intentionally `true` until the repository adds a package manifest or setup script.
- No local services are required or available to run in Cursor Cloud yet. When app code is added, update this section with any non-obvious run/test caveats and keep standard commands in the project README or package scripts.
